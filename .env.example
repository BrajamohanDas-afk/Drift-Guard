# Drift Guard environment examples
#
# Use `.env.docker.example` for docker compose.
# Use `.env.local.example` for host-side commands and tests.
#
# Copy the matching file to `.env`, then replace local development secrets before
# using the app outside your machine.
